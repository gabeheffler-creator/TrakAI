kind = "web"
previewPath = "/"
title = "Trak Coach"
version = "1.0.0"
id = "artifacts/trak-coach"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26086

[services.development]
run = "pnpm --filter @workspace/trak-coach run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trak-coach", "run", "build" ]
publicDir = "artifacts/trak-coach/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26086"
BASE_PATH = "/"
